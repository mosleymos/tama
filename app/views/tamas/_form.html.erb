<%= form_for(@tama) do |f| %>
  <% if @tama.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tama.errors.count, "error") %> prohibited this tama from being saved:</h2>

      <ul>
      <% @tama.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
